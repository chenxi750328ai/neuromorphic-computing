# FPGA 目标验证回执 · Loop（陈正共）

机读：`python3 scripts/phase4_fpga_goal_verify.py --gate` → `docs/phase4_poc_evidence/fpga_goal_verify_G1_G8.json`

## 得分：**8/8**（2026-08-02）

| Goal | 状态 | 证据 |
|------|------|------|
| G1 开源 lif bit | ✅ | `fpga_z2_openxc7_try_lif_axi.json` |
| G2 开源 bit 上板≡金标 | ✅ | `fpga_ra_atlas_mlif_inchain_gate.json` · bit=`lif_step_overlay_openxc7.bit` · match_rate=1.0 · acc=0.95 |
| G3 金标独立 | ✅ | FixedPointLIF 向量 + Verilator |
| G4 third_party.lock | ✅ | 仓根 |
| G5–G8 | ✅ | 同前 |

## 本轮关键发现

- RESET 后 eth0 IP 丢失 → 串口 `fpga-serial-setip.ps1` 恢复 `.3`
- 硬件狗存在：`cdns-wdt` timeout 10s，`/dev/watchdog`；systemd `RuntimeWatchdogSec=0`（未开）
- openXC7 无 BD：daemon 增加 MMIO@0x40000000 fallback
- AXI 分拍握手修复后 MMIO 不再冻核

## 狗与自动化（后续）

建议打开 `RuntimeWatchdogSec=10` 或常驻喂狗，避免再靠人工 RESET。
