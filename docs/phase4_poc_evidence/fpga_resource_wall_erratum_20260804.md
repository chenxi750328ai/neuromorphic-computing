# 资源口径勘误（2026-08-04）

> 总裁：「不可能装不下」。

| 旧说法 | 正确说法 |
|--------|----------|
| Z2 装不下整网 | **错**。装不下的是 **256 核并行 LIF**（379×256 LUT 外推） |
| 分时整网资源 | **够**：`time_mux_fits_z2=true`；单核 LIF ≈ **0.71%** LUT（379/53200） |
| F7 未通原因 | **欠工程**（fc*+LIF 进 PL + 调度），**不是**板太小 |

证据真源仍见 `fpga_rb_fullnet_platform_gate.json`（并行墙）；本勘误纠正话术，不改写历史 JSON 字段名。

